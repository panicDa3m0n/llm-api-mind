# V1.15.0 Memory Fields Checkpoint

Date: 2026-06-23

Scope: memory field semantics, retrieval ranking, surface compilation, KG
navigation, model-facing memory prompt.

## Backups

- System prompt backup:
  `backend/app/prompts/backups/scarlet_system.20260623T000000Z.pre-v1150-memory-fields.md`

## Settings Snapshot

These settings are expected to change in this slice:

- `RETRIEVAL_HYBRID_SALIENCE_WEIGHT`: static stored memory salience should no
  longer contribute to active ranking.
- `RETRIEVAL_HYBRID_CONFIDENCE_WEIGHT`: static stored memory confidence should no
  longer contribute to active ranking.

These settings remain outside this slice:

- `MINIMAX_MODEL`
- `DATABASE_URL`
- `CODEX_TEST`
- `OPENROUTER_API_KEY`
- runtime locale/profile settings

## Rollback Notes

To rollback the prompt only, restore the backup file above to
`backend/app/prompts/scarlet_system.md`.

To rollback ranking behavior, restore static salience/confidence weights and the
previous ranking multipliers from the Git diff for this checkpoint.

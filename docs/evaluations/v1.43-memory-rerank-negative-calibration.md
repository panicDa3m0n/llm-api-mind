# V1.43 Memory Rerank Negative Calibration

Date: 2026-07-18
Status: initial frozen checkpoint
Linear issue: SCA-31
Target version: V1.43.0

## Objective

Revalidate the final memory reranker against realistic personal negatives near
the accepted floor. Sparse, dense, graph, lexical, stored confidence, and
salience remain recall or diagnostic evidence only. The configured reranker
must remain the sole final relevance authority.

## Production Evidence

The read-only V1.42 inspection of production trace
`trace_604ee7213949446ea6883b4d2e2bffd2`, turn
`turn_b7f3a888f91d4b149eb93381faf412aa`, confirms the SCA-31 report:

- query: `te menta preferenza` (the stored trace retains the accented form);
- 64 initial memory candidates and 20 memory-level rerank candidates;
- 10 candidates evaluated by
  `nvidia/llama-nemotron-rerank-vl-1b-v2:free`;
- best result: Context Router memory
  `mem_e50803a35e4f44e5a66eaecc16d89a9a`, score
  `0.004101929257867252`;
- effective floor: `0.004`, therefore one false acceptance;
- no active production memory establishes a tea or mint preference.

The winning candidate entered through graph and lexical recall. Those routes
did not make the final decision; the defect is the observed separation at the
reranker acceptance boundary.

## Immutable Frozen Source

The reusable V1.37 source remains:

```txt
backend/data/v131-live-rerank-provenance-20260713.db
SHA-256 4fd51648530bb9cb0959fba0adc0a96ec9cf41eb35408373085ffbbe64535010
```

It contains 36 memories (35 active), 26 facts, 165 sessions, and 602 messages.
Every run starts from a fresh ignored `test` copy and verifies the source hash
before and after execution.

## Cases Frozen Before Policy Change

The eleven accepted V1.37 cases remain unchanged. Five natural personal
negatives are added before any threshold or query change:

| Case | Natural query | Expected result | Nearby distractor class |
|---|---|---|---|
| `unknown_landscape_preference` | `Ti ricordi se preferisco il mare o la montagna?` | no selected memory | generic preferences |
| `unknown_colleague_name` | `Come si chiama il mio collega Marco?` | no selected memory | known-person memories |
| `unknown_strawberry_allergy` | `Sono allergico alle fragole?` | no selected memory | food/body constraint |
| `unknown_running_routine` | `A che ora vado a correre di solito?` | no selected memory | personal routines/time |
| `unknown_favourite_colour` | `Qual e il mio colore preferito?` | no selected memory | generic user preferences |

Existing positive neighbors cover the mint evening drink, chocolate limit,
fatigue response style, Davide identity, report preference, entity
disambiguation, and multi-fact temporal recall. This keeps negative pressure
from being optimized without preserving nearby positive behavior.

## Acceptance

1. Every required positive reaches the recall pool, final selection, and V2
   sourceable hook.
2. Every negative returns an empty relevant-memory lane.
3. Trace evidence reports pool, routes, scores, effective threshold, provider,
   and latency.
4. No deterministic recall score or weighted fusion can accept a memory.
5. A policy change is allowed only if repeated current-provider evidence shows
   separation from both the production false positive and frozen positives.
6. Production remains read-only; direct Scarlet validation uses a disposable
   database and is limited to one positive and one negative unless evidence
   requires more.

## Initial Classification

The tea/mint event is not ordinary answer variance: a model-facing irrelevant
memory was deterministically delivered after the reranker score crossed the
configured floor. It is also not yet proof that graph recall, query
normalization, or the provider model must change. The first comparison must
measure the expanded frozen set on the current provider before selecting the
smallest correction.
